#ifndef MAP_HPP
# define MAP_HPP


# include "vector.hpp"

namespace ft {
	template < class Key,
		class value_type,
		class Compare = std::less<Key>,
		class Alloc = std::allocator<std::pair<const Key, value_type> > >
	class map {

	};

}

#endif
